<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.memo-mapper">
	<insert id="memoInsert" parameterType="MemoVO">
		INSERT INTO memo VALUES(
			(SELECT NVL(MAX(mno)+1,1) FROM memo),
			#{cno},#{cate},#{id},#{name},#{msg},SYSDATE
		)
	</insert>
	<select id="memoListData" resultType="MemoVO" parameterType="java.util.Map">
	    SELECT mno, cno, cate, id, name, msg, TO_CHAR(regdate, 'YYYY-MM-DD HH24:MI:SS') as dbday
	    FROM memo
	    WHERE id=#{id}
	</select>
	
	<delete id="memoDelete" parameterType="int">
		DELETE FROM memo
		WHERE id=${id}
	</delete>
	<update id="memoUpdate" parameterType="java.util.Map">
		UPDATE memo SET 
		msg=#{msg}
		WHERE id=${id}
	</update>
</mapper>